# 攻略上图｜Travel Notes to Map

## 1. 项目背景

这是一个面向小红书 AI 产品岗位面试的 3 天 Demo。

旅行前，用户经常会在小红书收藏大量攻略，但真正制定行程时仍然需要反复完成：

- 从攻略中手动抄地点
- 去地图 App 搜索地点
- 判断地点之间的距离
- 查看是否存在明显折返
- 再回到攻略里寻找拍照、餐饮、避坑等信息
- 根据自己的偏好重新调整路线

本产品希望利用 AI，将散落在攻略中的非结构化信息转化成真实地图上的旅行计划。

核心理念：

> 用户负责种草，AI 负责把它们串起来。

AI 不替用户决定“应该去哪”，而是帮助用户把已经产生的旅行意图转化成更容易执行的计划。

## 2. 产品目标

用户输入旅行攻略文本或截图后：

攻略文本 / 截图  
→ AI 理解内容  
→ 抽取地点  
→ 地图服务确认真实 POI  
→ 地图打点  
→ 用户调整地点优先级  
→ AI 理解旅行约束  
→ 路线重新组织并可视化

最终让用户从：

> “我收藏了很多地方，但不知道怎么安排。”

变成：

> “这些地方在哪、哪些顺路、应该怎么走，一张地图就能看懂。”

## 3. 核心用户场景

例如用户准备去上海旅行。

用户已经收藏了多篇攻略，其中提到：

- 武康大楼
- 安福路
- 上生新所
- 豫园
- 外滩
- 北外滩
- 若干咖啡店和餐厅

当前需要在小红书和地图之间反复切换。

使用本产品后：

1. 上传攻略截图 / 粘贴攻略文字
2. AI 自动识别其中的地点
3. 调用高德地图 POI 服务确认真实地点
4. 地点自动出现在地图上
5. 点击地点可以查看攻略中与该地点相关的信息
6. 用户将地点标记为：
   - 必去
   - 顺路就去
   - 删除
7. 用户补充：
   - 玩几天
   - 住在哪里
   - 是否接受早起
   - 步行偏好等
8. 系统帮助重新组织路线
9. 地图展示调整后的旅行路线

## 4. 核心产品原则

### 4.1 AI 负责理解，不负责制造事实

LLM 可以：

- 从文本中识别地点
- 从图片中理解攻略
- 判断地点类别
- 摘要攻略 Tips
- 理解用户自然语言偏好
- 帮助进行日程分组
- 解释为什么这样安排

LLM 不可以：

- 自己生成经纬度
- 编造真实 POI
- 编造距离
- 编造交通时间
- 编造营业时间
- 在存在多个同名 POI 时擅自猜测

所有真实地理信息必须通过地图服务 Grounding。

### 4.2 确定性问题尽量交给工具

架构原则：

攻略文本 / 图片  
↓  
LLM  
↓  
结构化 Place 数据  
↓  
高德 POI Search  
↓  
真实地点 + 经纬度  
↓  
前端地图 Marker

LLM 只输出地点名称和语义信息，不直接输出坐标。

### 4.3 用户保留最终决策权

AI 不应该独自制定最终行程。

用户可以随时：

- 标记必去
- 标记顺路
- 删除地点
- 修改地点
- 选择同名 POI
- 修改旅行约束
- 重新规划路线

## 5. MVP 功能优先级

### P0：必须实现

#### P0-1 真实地图展示

接入高德地图。

需要支持：

- 地图正常加载
- 指定城市定位
- Marker 展示
- Marker 点击
- 自动调整地图视野，使所有 Marker 可见

#### P0-2 POI Grounding

输入：

- 城市
- 地点名称

例如：

城市：上海

地点：

- 武康大楼
- 安福路
- 外滩

调用高德 POI Search / 地理服务，获取：

- POI 名称
- 地址
- 经纬度

然后在地图上展示真实 Marker。

禁止使用手写坐标。

#### P0-3 攻略文本解析

提供一个文本输入区域。

用户可以粘贴类似：

“第一天建议从武康大楼开始，沿武康路走到安福路，中午可以去附近喝咖啡。如果还有时间可以去上生新所。第二天可以去豫园，再去外滩看夜景。”

LLM 输出固定 JSON Schema。

示例：

```json
{
  "city": "上海",
  "places": [
    {
      "name": "武康大楼",
      "category": "景点",
      "source": "攻略1",
      "tips": ["建议上午前往"]
    },
    {
      "name": "安福路",
      "category": "街区",
      "source": "攻略1",
      "tips": ["可以和武康路一起游览"]
    }
  ]
}
```

然后：

place.name  
→ 高德 POI Search  
→ 经纬度  
→ Marker

### P1：推荐实现

#### P1-1 图片上传

用户可以上传攻略截图。

多模态模型解析截图。

图片解析结果必须和文本解析使用同一个 Place JSON Schema。

不要为图片单独创建另一套地图处理逻辑。

#### P1-2 地点卡片

地图旁展示地点列表。

每个地点包含：

- 地点名称
- 分类
- 攻略 Tips
- 来源
- 当前优先级

优先级：

❤️ 必去  
👀 顺路就去  
× 删除

地图 Marker 和地点卡片保持联动。

#### P1-3 POI 消歧

如果高德 POI Search 返回多个合理结果：

不要自动选择。

向用户展示候选地点，例如：

你说的是哪个？

- %Arabica 武康路店
- %Arabica 新天地店

用户确认后再打点。

### P2：时间允许再做

#### P2-1 旅行约束

用户可以输入：

- 旅行天数
- 住宿地点
- 大致出发时间
- 不想早起
- 希望少打车
- 更喜欢步行
- 哪些地点必须去

模型负责把自然语言转成结构化约束。

#### P2-2 行程分组

根据：

- 地理位置
- 地点优先级
- 用户约束

将地点分为：

Day 1  
Day 2  
Day 3

不要求实现数学意义上的全球最优解。

优先保证：

- 避免明显折返
- 相近地点尽量安排在同一天
- 必去地点优先满足

#### P2-3 路线展示

调用高德路线规划能力。

地图展示：

地点顺序，以及地点之间的路线。

如果路线规划 API 实现成本过高，可以先使用简单连线作为 Demo 兜底，但真实 POI 和 Marker 必须保留。

## 6. 明确不做

三天 Demo 内禁止主动扩展以下需求：

- 小红书 URL 自动抓取
- 小红书账号授权
- 小红书收藏夹同步
- 用户登录
- 用户注册
- 数据库
- 云端用户数据存储
- 社交功能
- 好友协作
- 评论系统
- 支付功能
- 酒店预订
- 机票预订
- 复杂推荐系统
- 全局旅行最优路线算法
- 商业级风控
- 商业级地图调用优化

除非核心 MVP 已经完全稳定，否则不要开发上述功能。

## 7. 页面结构

优先做单页 Web App。

推荐桌面端布局：

左侧约 35%：

- Logo / 产品名
- 攻略输入
- 图片上传
- AI 解析状态
- 地点卡片
- 用户偏好

右侧约 65%：

- 高德地图
- Marker
- 路线
- 当前 Day 标签

避免设计成普通 AI Chatbot。

## 8. UI 风格

关键词：

- 小红书
- 旅行
- 内容卡片
- 地图
- 手帐感
- 轻量
- 温暖
- 生活方式

避免：

- 蓝紫渐变
- 大面积黑色
- Cyberpunk
- AI SaaS Dashboard
- ChatGPT 克隆界面
- 复杂后台管理系统感

界面核心应该是：

“攻略内容”和“地图”的结合。

## 9. 推荐交互

首页文案：

> 把收藏的攻略，摊到地图上。

副标题：

> 你负责种草，AI 帮你把它们串起来。

操作：

粘贴攻略 / 上传截图

按钮：

> 帮我上图

解析过程中：

> 正在从攻略里找你种草的地方……

地图出现地点后：

> 找到了 12 个地点

允许用户：

❤️ 必去  
👀 顺路  
× 删除

路线按钮：

> 帮我顺一下

## 10. 数据结构建议

核心 Place：

```ts
interface Place {
  id: string;
  rawName: string;
  name?: string;
  category?: string;
  source?: string;
  tips?: string[];

  priority?: "must" | "optional" | "removed";

  poiId?: string;
  address?: string;
  longitude?: number;
  latitude?: number;

  poiStatus:
    | "unresolved"
    | "resolved"
    | "ambiguous"
    | "not_found";

  poiCandidates?: POICandidate[];
}
```

AI Extraction Result：

```ts
interface ExtractionResult {
  city?: string;
  places: ExtractedPlace[];
}
```

其中：

```ts
interface ExtractedPlace {
  name: string;
  category?: string;
  source?: string;
  tips?: string[];
}
```

注意：

ExtractedPlace 中禁止包含经纬度。

## 11. 错误处理

必须考虑：

### LLM 输出异常

如果不是合法 JSON：

- 重试一次
- 或进行结构化修复
- 不允许前端直接崩溃

### POI 未找到

地点卡片显示：

> 暂时没有找到这个地点

允许用户修改名称。

### POI 多结果

显示候选结果。

禁止模型自行猜测。

### 地图加载失败

显示明确错误提示。

不要出现空白页面。

## 12. 技术实现原则

优先：

- 简单
- 稳定
- 可演示
- 少依赖
- 少状态
- 少抽象

不要为了“工程优雅”过度设计。

但需要保证：

- AI 层
- POI Grounding 层
- Map 展示层

三者逻辑分离。

图片和文本输入必须最终复用同一条：

parse → places → resolve POI → map

链路。

## 13. Demo 成功标准

最核心 Demo：

用户粘贴一段上海攻略。

点击：

> 帮我上图

几秒后：

地图中出现：

- 武康大楼
- 安福路
- 上生新所
- 外滩
- 豫园

等真实地点。

用户能够点击 Marker 查看攻略 Tips。

如果完成这一点，MVP 已经达到可提交状态。

## 14. 开发优先级

严格遵循：

1. 高德真实地图加载
2. 固定地点 POI Search
3. Marker 打点
4. Marker 点击信息
5. 文本 → AI → Place JSON
6. Place JSON → POI → Marker
7. 图片上传
8. 地点优先级
9. 路线规划
10. UI 美化

禁止在 1–6 未稳定之前开发 7–10。

## 15. 三天计划

### Day 1

目标：

固定地点 → 高德 POI → 地图 Marker

至少完成上海：

- 武康大楼
- 安福路
- 外滩

三个地点真实出现。

### Day 2

目标：

攻略 → AI → 地图

完成：

文本输入  
→ LLM Extraction  
→ Place JSON  
→ POI Search  
→ Marker

如果稳定，再增加图片。

### Day 3

目标：

提高 Demo 表现力。

按优先级：

1. 必去 / 顺路 / 删除
2. 路线重排
3. 地点详情
4. UI 优化
5. Loading / Error
6. 准备稳定 Demo 数据
7. 截图和录屏

Day 3 不再进行大型架构改造。

## 16. 最重要的开发纪律

任何时刻，都必须保留一个可以完整演示的版本。

不要为了开发下一阶段功能破坏已经工作的链路。

如果新功能导致核心流程不稳定：

优先回退。

对于本项目：

> 可演示性 > 功能数量 > 工程复杂度。
